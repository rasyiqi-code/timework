{
  "name": "timework-monorepo",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "latest"
  },
  "packageManager": "pnpm@9.0.0",
  "pnpm": {
    "peerDependencyRules": {
      "allowedVersions": {
        "react": "19",
        "react-dom": "19",
        "@next/eslint-plugin-next": "16"
      }
    }
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@types/react-dom": "^19"
  }
}